# ========================
# DEV (로컬 개발용)
# ========================

# 프론트 주소
app.cors.allowed-origins=http://localhost:3000
app.oauth2.frontend-redirect=http://localhost:3000/oauth/callback

# JWT (로컬은 기본값 허용)
app.jwt.secret=${JWT_SECRET:dev-secret-change-me}
app.jwt.issuer=goodsugar
app.jwt.access-minutes=15
app.jwt.refresh-days=14
