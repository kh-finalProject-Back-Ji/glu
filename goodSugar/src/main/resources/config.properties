# File로 생성해서 이름을 config.properties 라고 작성항여 생성
# *** .gitignore 파일에 config.properties 작성확인 ****

#오라클 DB 연결 정보


spring.datasource.url=jdbc:oracle:thin:@1.223.77.182:12345:XE 
spring.datasource.hikari.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.hikari.jdbc-url=jdbc:oracle:thin:@1.223.77.182:12345:XE
spring.datasource.hikari.username=baekji
spring.datasource.hikari.password=12121
# HikariCP Connection Pool Properties
#커넥션(연결)을 사용할 수 있을 때까지 대기하는 최대 시간(ms)을 지정
spring.datasource.hikari.connection-timeout=30000

#풀의 최대 연결 수 설정
spring.datasource.hikari.maximum-pool-size=5

#연결이 풀에서 유휴 상태로 있을 수 있는 최대 시간(ms)을 지정
spring.datasource.hikari.idle-timeout=600000

#연결 풀의 이름을 지정
spring.datasource.hikari.pool-name=MyHikariCP

#자동 커밋 끄기
spring.datasource.hikari.auto-commit=false


spring.mail.username=chulkuji12@gmail.com
spring.mail.password=quidqouqttmovcqr


# (참고)
# 1MB = 1024KB
# 1KB = 1024B

# 파일을 디스크에 쓸 때까지의 임계 값
# 기본 값 : 0B
# 50MB로 변경 == 52,428,800 B
# -> 업로드되는 파일의 크기 50MB 까지는 메모리에 임시로 저장하다가
# 초과 시 디스크(HDD, SSD)에 저장
spring.servlet.multipart.file-size-threshold=52428800

# 임계 값 초과 시 파일의 임시 저장 경로
# 마지막에 / 꼭 작성해줘야 함
spring.servlet.multipart.location=C:/uploadFiles/temp/

# HTTP 요청당 최대 크기
# 기본 값 : 10MB
# 50MB 로 변경
spring.servlet.multipart.max-request-size=52428800

# 개별 파일당 최대 크기
# 기본 값 : 1MB
# 10MB 로 변경
spring.servlet.multipart.max-file-size=10485760

#----------------------------------------

# 프로필 이미지 요청 주소 (FileConfig)
my.profile.resource-handler=/myPage/profile/**

# 프로필 이미지 요청 시 연결될 서버 폴더 경로 (FileConfig)
my.profile.resource-location=file:///C:/uploadFiles/profile

# 서비스에서 프로필 이미지 요청 주소를 조합할 때 사용하는 경로(MyPageServiceImpl)
my.profile.web-path=/myPage/profile/

# 서비스에서 파일 저장(transferTo()) 시 사용할 폴더 경로(MyPageServiceImpl)
my.profile.folder-path=C:/uploadFiles/profile/

#----------------------------------------------

#게시글 이미지 요청 주소 : 클라이언트가 어떤 미미지를 보고싶어할 떄 요청 보낼때 
#(FileConfig)
my.board.resource-handler=/images/board/**

#게시글 이미지 요청 시 연결 될 폴더 경로
my.board.resource-location=file:///c:/uploadFiles/board/

#게시글 이미지 요청 주소 (DB 저장용)
my.board.web-path=/images/board/

#rptlrmf dlalwlfmf tjqjdp wjwkdgkf EO tkdydgkf rudfh
my.board.folder-path=c:/uploadfiles/board/


#log 파일이 저장될 경로
log.config.path=c:/log/

# log파일의 이름
log.config.fileName=boardProject-log
